import React from "react";
import CampaignList from "./campaignList";
import { Container } from "react-bootstrap";

const Home = () => (
  <div>
    <Container>
      <CampaignList />
    </Container>
  </div>
);

export default Home;
